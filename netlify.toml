[build]
  command = "cd site && hugo"
  publish = "site/public"

[build.environment]
  HUGO_VERSION = "0.111.3"

[context.production.environment]
  HUGO_ENV = "production"

[context.deploy-preview]
  command = "cd site && hugo -D -F"

[context.branch-deploy]
  command = "cd site && hugo -D -F"